name: suite-name

fs:
  files:
    - name: parentFile
      vpath: parent.txt
      seed_type: random
      seed_length: 30
      pre_run_write: true

units:
  case1:
    src: assets/testing/fs-test.yml