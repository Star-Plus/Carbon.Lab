#include <gtest/gtest.h>
// #include "components/ConfigurationParser.h"

TEST(C14ParsingTest, ShallowUnitParsing) {
    // auto parser = CarbonLab::ConfigurationParser("assets/testing/unit-test.yml");
    // auto carbon = parser.load("case1");
    // EXPECT_EQ(carbon.fs->files().size(), 2);
}